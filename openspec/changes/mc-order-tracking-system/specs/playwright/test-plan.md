# Test Plan — mc-order-tracking-system

Generated: 2026-05-21T18:45:00Z

## Summary

- **Auth**: 无需认证（角色切换为前端 toggle）
- **Roles**: 顾客（默认）、经理
- **Routes**: `/`（单页 SPA）
- **Processing Time**: 10 秒/单

## Test Cases

### 角色管理 (manager-role)

- ✅ `默认角色为顾客` — `/`, 页面加载后默认显示"顾客"为激活状态，+/- Bot 和活动日志不可见
- ✅ `切换到经理角色` — `/`, 点击"经理"按钮后，+/- Bot 和活动日志面板可见
- ✅ `从经理切换回顾客` — `/`, 点击"顾客"按钮后，+/- Bot 和活动日志面板隐藏
- ✅ `切换角色后数据保持` — `/`, 经理角色下创建机器人+订单，切换顾客再切回经理，数据完整

### 订单提交 (order-submission)

- ✅ `创建普通订单` — `/`, 顾客角色点击"New Normal Order"，订单出现在 PENDING 区域
- ✅ `创建 VIP 订单` — `/`, 顾客角色点击"New VIP Order"，订单出现在 PENDING 区域
- ✅ `订单号递增` — `/`, 连续创建 3 个订单，订单号依次为 #1, #2, #3
- ✅ `经理也可创建订单` — `/`, 经理角色创建订单，订单正常创建
- ✅ `日志记录订单创建` — `/`, 创建订单后，切换经理，活动日志显示创建记录

### 订单队列管理 (order-queue-management)

- ✅ `PENDING 区域空状态` — `/`, 无订单时显示"暂无待处理订单"
- ✅ `COMPLETE 区域空状态` — `/`, 无订单时显示"暂无已完成订单"
- ✅ `VIP 订单优先展示` — `/`, 先创建 Normal, 再创建 VIP，PENDING 中 VIP 排在前面
- ✅ `订单完成后移入 COMPLETE` — `/`, 创建订单等 10 秒后，订单从 PENDING 消失，出现在 COMPLETE
- ✅ `角色切换不影响订单视图` — `/`, 有订单时切换角色，PENDING/COMPLETE 内容不变

### 机器人管理 (bot-management)

- ✅ `经理增加机器人` — `/`, 经理点击"+ Bot"，机器人创建并显示状态
- ✅ `经理减少 IDLE 机器人` — `/`, 增加机器人后点击"- Bot"，机器人被销毁
- ✅ `无机器人时 -Bot 禁用` — `/`, 无机器人时"- Bot"按钮为禁用状态
- ✅ `顾客不可见 +/- Bot 按钮` — `/`, 顾客角色下 +Bot/-Bot 按钮不存在
- ✅ `顾客可见机器人状态` — `/`, 经理创建机器人后切到顾客，机器人状态可见但无操作按钮
- ✅ `机器人自动取单处理` — `/`, 有 IDLE 机器人和 PENDING 订单时，订单被自动取走处理
- ✅ `机器人状态展示处理中` — `/`, 机器人取单后显示"处理中: 订单 #N (类型)"
- ✅ `机器人处理完成后自动取下一单` — `/`, 1 个机器人 + 2 个 PENDING 订单，两个都被处理
- ✅ `VIP 优先取单` — `/`, VIP+Normal 同时 PENDING，机器人优先取 VIP

### 活动日志 (activity-log)

- ✅ `日志面板仅经理可见` — `/`, 顾客角色下活动日志面板不存在
- ✅ `日志面板经理可见` — `/`, 经理角色下活动日志面板可见，包含所有历史记录
- ✅ `日志倒序排列` — `/`, 多条日志按时间从新到旧排列
- ✅ `日志记录订单创建事件` — `/`, 创建订单后日志显示"订单 #N 已创建"
- ✅ `日志记录机器人上线/下线` — `/`, 增删机器人后日志显示"机器人 #M 已上线/下线"
- ✅ `日志记录订单处理开始/完成` — `/`, 机器人处理订单后日志显示处理和完成记录

### 机器人销毁回退 (bot-management - 高级场景)

- ✅ `销毁处理中的机器人订单退回` — `/`, 有 1 个机器人正在处理订单时，经理点击"- Bot"，订单退回 PENDING，日志显示退回记录

## Not Covered

- 日志 200 条上限：需要创建 200+ 订单，测试时间过长，跳过
- 多机器人并行处理细节：2 个机器人 + 2 个订单并行处理，关键行为已由 VIP 优先取单覆盖
