## 1. 修改 Bot 类的 destroy() 方法

- [x] 1.1 修改 `Bot.destroy()` 方法，移除忙碌状态检查
- [x] 1.2 在销毁时清理定时器（如果存在）
- [x] 1.3 修改返回值，包含当前正在处理的订单（如果有）

## 2. 修改 OrderController 的 removeBot() 方法

- [x] 2.1 修改 `OrderController.removeBot()` 方法，接收机器人销毁返回的订单
- [x] 2.2 如果订单存在且状态为 processing，更新状态为 pending
- [x] 2.3 将订单重新插入优先级队列（保持原始创建时间）
- [x] 2.4 触发 `processPendingOrders()` 重新分配订单

## 3. 更新测试用例

- [x] 3.1 为机器人销毁逻辑编写测试用例
- [x] 3.2 为订单回退逻辑编写测试用例
- [x] 3.3 运行测试确保修改正确

## 4. 验证

- [x] 4.1 在本地测试机器人移除功能
- [x] 4.2 确保忙碌机器人被移除时订单正确回退到队列