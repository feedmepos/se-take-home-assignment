# Backend Environment Variables Example
# Copy this file to .env and update with your values
# 
# cp .env.example .env
# Then edit .env with your actual values

# Server Port
PORT=3000

# JWT Secret Key for authentication
# Generate a secure random string for production
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# Database URL (for Prisma - not used since we're using in-memory storage per requirement)
# Uncomment and configure if you want to use PostgreSQL in the future
# DATABASE_URL=postgresql://user:password@localhost:5432/mcdonalds_orders?schema=public
DATABASE_URL="postgresql://wengyewong:P@ssw0rd-=@localhost:5432/feedme_assessment?schema=public"

