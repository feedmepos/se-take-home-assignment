{
  "name": "order-controller-backend",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "order-controller": "src/cli.js"
  },
  "scripts": {
    "build": "node --check src/order-controller.js && node --check src/cli.js && mkdir -p dist && cp src/*.js dist/",
    "start": "node src/cli.js --demo",
    "test": "node --test test/*.test.js"
  }
}
