export default {
  plugins: {
    '@tailwindcss/postcss': {},
    // In v4, imports and vendor prefixing are often handled automatically,
    // so you might not need autoprefixer or postcss-import anymore.
    // If you still require it for other purposes, keep it.
    // autoprefixer: {},
  },
}
