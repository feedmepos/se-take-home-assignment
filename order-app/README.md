## Order App

This prototype relies on in-memory state and is intended to run as a single process. Deploying to serverless or multi-instance environments will result in inconsistent state because memory is not shared between instances