// 订单状态
export enum OrderStatus {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED'
}

// 订单类型
export enum OrderType {
  NORMAL = 'NORMAL',
  VIP = 'VIP'
}

// 机器人状态
export enum BotStatus {
  IDLE = 'IDLE',
  PROCESSING = 'PROCESSING'
}