/** Bot processing duration per order (ms) */
export const PROCESSING_TIME_MS = 10_000;

/** Processing engine tick interval (ms) — matches progress bar update rate */
export const TICK_INTERVAL_MS = 200;

/** Order numbers wrap to 0001 after this value */
export const ORDER_NUMBER_WRAP = 9_999;

/** Zero-padded display width for order numbers */
export const ORDER_NUMBER_PADDING = 4;
