# Generated by scripts/run.sh on every run (and in CI before the verify step).
# Not tracked — see scripts/result.example.txt for a sample of the format.
scripts/result.txt

# Frontend build output (Vite emits here)
frontend-dist/

# Frontend dependencies
frontend/node_modules/
