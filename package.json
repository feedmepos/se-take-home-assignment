{
  "name": "mcdonald-order-queue",
  "version": "1.0.0",
  "description": "McDonald's order queue system with CLI and REST/WebSocket server",
  "main": "src/index.js",
  "scripts": {
    "cli": "node src/index.js --cli",
    "server": "node src/index.js --server",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  }
}
