{
  "name": "feedme-order-controller",
  "version": "1.0.0",
  "description": "McDonald's automated cooking bot order controller — FeedMe take-home assignment",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "node --check src/OrderController.js && node --check src/Logger.js && node --check src/index.js && node --check src/tests.js && echo 'Build OK ✅'",
    "test": "cross-env ORDER_PROCESS_MS=100 node src/tests.js"
 },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}