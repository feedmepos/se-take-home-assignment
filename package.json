{
  "name": "order-controller",
  "version": "1.0.0",
  "description": "McDonald's order controller CLI (cooking bots) - FeedMe take home",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --test \"dist/test/**/*.test.js\"",
    "start": "node dist/cli.js",
    "simulate": "node dist/cli.js --simulate"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.6.0"
  }
}
